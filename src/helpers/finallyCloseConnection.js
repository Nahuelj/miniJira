import { closeConectionDB } from "@/DB/connection";

export async function finallyCloseConnection() {
  // try {
  //   await closeConectionDB();
  // } catch (error) {
  //   console.log(error);
  //   return res(500);
  // }
}
